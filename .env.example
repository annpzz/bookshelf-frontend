# ตัวอย่างไฟล์ .env สำหรับ Frontend
# สำหรับ dev ไม่ต้องสร้างไฟล์นี้ (จะใช้ localhost:3000 อัตโนมัติ)
# สำหรับ Production (Vercel) ตั้ง env var นี้ใน dashboard ของ Vercel:
VITE_API_URL=https://your-backend.onrender.com
